#include <iostream> // Include the iostream library for input/output functionality

using namespace std; // Use the std namespace to avoid having to prefix cout with std::

int main() // Define the main function, which is the entry point of the program
{
   // Print "hello world" to the console
   cout << "hello world";

   // Return 0 to indicate successful execution
   return 0;
}